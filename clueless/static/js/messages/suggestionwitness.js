// source: messages.proto
/**
 * @fileoverview
 * @enhanceable
 * @suppress {messageConventions} JS Compiler reports an error if a variable or
 *     field starts with 'MSG_' and isn't a translatable message.
 * @public
 */
// GENERATED CODE -- DO NOT EDIT!

goog.provide('proto.SuggestionWitness');

goog.require('jspb.BinaryReader');
goog.require('jspb.BinaryWriter');
goog.require('jspb.Message');
goog.require('proto.WitnessResponseItem');

goog.forwardDeclare('proto.Character');
/**
 * Generated by JsPbCodeGenerator.
 * @param {Array=} opt_data Optional initial data array, typically from a
 * server response, or constructed directly in Javascript. The array is used
 * in place and becomes part of the constructed object. It is not cloned.
 * If no data is provided, the constructed object will be empty, but still
 * valid.
 * @extends {jspb.Message}
 * @constructor
 */
proto.SuggestionWitness = function(opt_data) {
  jspb.Message.initialize(this, opt_data, 0, -1, null, null);
};
goog.inherits(proto.SuggestionWitness, jspb.Message);
if (goog.DEBUG && !COMPILED) {
  /**
   * @public
   * @override
   */
  proto.SuggestionWitness.displayName = 'proto.SuggestionWitness';
}



if (jspb.Message.GENERATE_TO_OBJECT) {
/**
 * Creates an object representation of this proto.
 * Field names that are reserved in JavaScript and will be renamed to pb_name.
 * Optional fields that are not set will be set to undefined.
 * To access a reserved field use, foo.pb_<name>, eg, foo.pb_default.
 * For the list of reserved names please see:
 *     net/proto2/compiler/js/internal/generator.cc#kKeyword.
 * @param {boolean=} opt_includeInstance Deprecated. whether to include the
 *     JSPB instance for transitional soy proto support:
 *     http://goto/soy-param-migration
 * @return {!Object}
 */
proto.SuggestionWitness.prototype.toObject = function(opt_includeInstance) {
  return proto.SuggestionWitness.toObject(opt_includeInstance, this);
};


/**
 * Static version of the {@see toObject} method.
 * @param {boolean|undefined} includeInstance Deprecated. Whether to include
 *     the JSPB instance for transitional soy proto support:
 *     http://goto/soy-param-migration
 * @param {!proto.SuggestionWitness} msg The msg instance to transform.
 * @return {!Object}
 * @suppress {unusedLocalVariables} f is only used for nested messages
 */
proto.SuggestionWitness.toObject = function(includeInstance, msg) {
  var f, obj = {
    character: jspb.Message.getFieldWithDefault(msg, 1, 0),
    item: (f = msg.getItem()) && proto.WitnessResponseItem.toObject(includeInstance, f)
  };

  if (includeInstance) {
    obj.$jspbMessageInstance = msg;
  }
  return obj;
};
}


/**
 * Deserializes binary data (in protobuf wire format).
 * @param {jspb.ByteSource} bytes The bytes to deserialize.
 * @return {!proto.SuggestionWitness}
 */
proto.SuggestionWitness.deserializeBinary = function(bytes) {
  var reader = new jspb.BinaryReader(bytes);
  var msg = new proto.SuggestionWitness;
  return proto.SuggestionWitness.deserializeBinaryFromReader(msg, reader);
};


/**
 * Deserializes binary data (in protobuf wire format) from the
 * given reader into the given message object.
 * @param {!proto.SuggestionWitness} msg The message object to deserialize into.
 * @param {!jspb.BinaryReader} reader The BinaryReader to use.
 * @return {!proto.SuggestionWitness}
 */
proto.SuggestionWitness.deserializeBinaryFromReader = function(msg, reader) {
  while (reader.nextField()) {
    if (reader.isEndGroup()) {
      break;
    }
    var field = reader.getFieldNumber();
    switch (field) {
    case 1:
      var value = /** @type {!proto.Character} */ (reader.readEnum());
      msg.setCharacter(value);
      break;
    case 2:
      var value = new proto.WitnessResponseItem;
      reader.readMessage(value,proto.WitnessResponseItem.deserializeBinaryFromReader);
      msg.setItem(value);
      break;
    default:
      reader.skipField();
      break;
    }
  }
  return msg;
};


/**
 * Serializes the message to binary data (in protobuf wire format).
 * @return {!Uint8Array}
 */
proto.SuggestionWitness.prototype.serializeBinary = function() {
  var writer = new jspb.BinaryWriter();
  proto.SuggestionWitness.serializeBinaryToWriter(this, writer);
  return writer.getResultBuffer();
};


/**
 * Serializes the given message to binary data (in protobuf wire
 * format), writing to the given BinaryWriter.
 * @param {!proto.SuggestionWitness} message
 * @param {!jspb.BinaryWriter} writer
 * @suppress {unusedLocalVariables} f is only used for nested messages
 */
proto.SuggestionWitness.serializeBinaryToWriter = function(message, writer) {
  var f = undefined;
  f = message.getCharacter();
  if (f !== 0.0) {
    writer.writeEnum(
      1,
      f
    );
  }
  f = message.getItem();
  if (f != null) {
    writer.writeMessage(
      2,
      f,
      proto.WitnessResponseItem.serializeBinaryToWriter
    );
  }
};


/**
 * optional Character character = 1;
 * @return {!proto.Character}
 */
proto.SuggestionWitness.prototype.getCharacter = function() {
  return /** @type {!proto.Character} */ (jspb.Message.getFieldWithDefault(this, 1, 0));
};


/**
 * @param {!proto.Character} value
 * @return {!proto.SuggestionWitness} returns this
 */
proto.SuggestionWitness.prototype.setCharacter = function(value) {
  return jspb.Message.setProto3EnumField(this, 1, value);
};


/**
 * optional WitnessResponseItem item = 2;
 * @return {?proto.WitnessResponseItem}
 */
proto.SuggestionWitness.prototype.getItem = function() {
  return /** @type{?proto.WitnessResponseItem} */ (
    jspb.Message.getWrapperField(this, proto.WitnessResponseItem, 2));
};


/**
 * @param {?proto.WitnessResponseItem|undefined} value
 * @return {!proto.SuggestionWitness} returns this
*/
proto.SuggestionWitness.prototype.setItem = function(value) {
  return jspb.Message.setWrapperField(this, 2, value);
};


/**
 * Clears the message field making it undefined.
 * @return {!proto.SuggestionWitness} returns this
 */
proto.SuggestionWitness.prototype.clearItem = function() {
  return this.setItem(undefined);
};


/**
 * Returns whether this field is set.
 * @return {boolean}
 */
proto.SuggestionWitness.prototype.hasItem = function() {
  return jspb.Message.getField(this, 2) != null;
};


